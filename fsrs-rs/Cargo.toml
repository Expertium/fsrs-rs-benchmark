[package]
name = "fsrs"
version = "6.0.0"
authors = ["Open Spaced Repetition"]
categories = ["algorithms", "science"]
edition = "2024"
homepage = "https://github.com/open-spaced-repetition"
keywords = ["spaced-repetition", "algorithm", "fsrs", "machine-learning"]
license = "BSD-3-Clause"
readme = "README.md"
repository = "https://github.com/open-spaced-repetition/fsrs-rs"
description = "FSRS for Rust, including Optimizer and Scheduler"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies.burn]
version = "0.17.1"
# git = "https://github.com/tracel-ai/burn.git"
# rev = "6ae3926006872a204869e84ffc303417c54b6b7f"
# path = "../burn/burn"
default-features = false
features = ["std", "train", "ndarray", "metrics"]

[dev-dependencies.burn]
version = "0.17.1"
# git = "https://github.com/tracel-ai/burn.git"
# rev = "6ae3926006872a204869e84ffc303417c54b6b7f"
# path = "../burn/burn"
default-features = false
features = ["std", "train", "ndarray", "sqlite-bundled", "metrics"]

[dependencies]
itertools = "0.14.0"
log = "0.4"
ndarray = "0.17.2"
rand = "0.9.2"
serde = "1.0.228"
snafu = "0.8.9"
# Portable explicit SIMD (safe_arch under the hood: SSE2/AVX on x86, NEON on ARM, scalar
# fallback elsewhere) — used to vectorize the analytic forward across 8 cards per f32x8 lane.
# Constraint 7-safe: no AVX-512, no nightly std::simd; degrades to portable scalar.
wide = "0.7"
bytemuck = "1"  # zero-cost bit reinterpret between f32x8 and i32x8 (both Pod) for exp/ln

[dev-dependencies]
chrono = { version = "0.4.43", default-features = false, features = ["std", "clock"] }
